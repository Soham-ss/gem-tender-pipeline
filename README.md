# IndiaMART to GeM Tender Automation Pipeline

Full pipeline for IndiaMART & GeM portal tender extraction to Google Sheets.

## Quick Start

1. Install requirements:
   pip install -r requirements.txt
   playwright install chromium

2. Add your service_account.json to this directory.
3. Run pipeline:
   python pipeline.py
